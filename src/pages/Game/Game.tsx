import React from "react";
import SideBar from "../../components/SideBar/SideBar";

type Props = {};

const Game = (props: Props) => {
  return (
    <div>
      <SideBar />
    </div>
  );
};
export default Game;
